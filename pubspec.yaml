name: seasalt
description: An e621 mobile app made with Flutter.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.12.0+2
  photo_view: ^0.4.2
  cached_network_image: ^1.1.1

flutter:
  uses-material-design: true
